<section id="sponsors-page">
  <div class="page-header">
    <h1>Sponsors</h1>
  </div>
  <div class="row">
    <div class="span12">
      <% @sponsors.each do |sponsor| %>
        <a href="<%= sponsor[0] %>"><img src="<%= sponsor[1] %>"/></a>
      <% end %>
    </div>
  </div>

</section>

